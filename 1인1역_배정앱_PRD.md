# 1인 1역 배정 앱 — PRD (초안 v0.1)

> 작성 기준: 2026-08-28 / 음성 인터뷰 정리본

---

## 1. 배경

학급 담임은 학기 초에 학생 한 명당 하나씩 역할(1인 1역)을 배정한다. 현재는 종이 조사나 구두 조사로 지망을 받고, 겹치면 가위바위보나 제비뽑기로 정하는 방식이 일반적이다. 이 과정은 시간이 오래 걸리고, 학급 전체 만족도가 아니라 국지적인 승패로 결정되기 때문에 결과가 최적이 아니다.

이 앱은 학생에게 1~3지망을 받아 **학급 전체의 만족도를 최대화하는 배정안**을 계산하고, 교사가 이를 검토·수정·확정할 수 있게 돕는다.

---

## 2. 목표 / 비목표

### 목표
- 학생 지망 수집을 QR 코드 한 장으로 끝낸다
- 경합을 자동으로 해소하되, 그 기준이 학생에게 설명 가능해야 한다
- 교사가 배정안을 여러 번 생성·비교하고 최종 확정할 수 있다
- 결과에 대한 최종 결정권은 항상 교사에게 있다

### 비목표 (v1 제외)
- 학생 화면에서의 결과 조회 (교실 분위기 관리를 위해 의도적으로 제외)
- 역할 수행 평가, 출결 연동, 이력 누적 기반 가중치
- 여러 학기에 걸친 역할 이력 관리

---

## 3. 사용자

| 사용자 | 사용 맥락 | 필요한 것 |
|---|---|---|
| 담임 교사 | PC 또는 태블릿, 교실/교무실 | 학급 개설, 명단·역할 설정, 배정 실행, 결과 확정 |
| 학생 | 개인 스마트폰, 교실에서 5분 이내 | QR 스캔 → 학번 입력 → 1~3지망 선택 → 제출 |

**멀티 테넌트 전제.** 여러 교사가 각자 계정으로 접속해 자기 학급을 개설한다. 3학년 1반 담임과 3학년 2반 담임이 같은 앱을 쓰되 서로의 데이터는 보이지 않는다.

---

## 4. 핵심 플로우

```
[교사] 로그인 → 학급 개설 → 명단 등록 → 역할·정원 설정
                                              ↓
                                    지망 접수 오픈 → QR 표시
                                              ↓
[학생] QR 스캔 → 학번 입력 → 이름 확인 → 1~3지망 선택 → 제출
                                              ↓
[교사] 실시간 제출 현황 확인 → 접수 마감
                                              ↓
                          배정 실행 (여러 번 가능) → 배정안 비교
                                              ↓
                          수동 조정 → 최종 확정 → 교실에서 발표
```

---

## 5. 기능 요구사항

### 5.1 교사 인증
- Google 계정 로그인 (Firebase Auth)
- 교사 1명이 여러 학급을 개설·관리 가능
- 학급 데이터는 개설한 교사 계정에만 귀속

### 5.2 학급 개설
- 학급명 입력 (예: 3학년 1반)
- 학급별로 명단·역할·지망·배정안이 독립적으로 관리됨

### 5.3 학생 명단 등록
두 가지 입력 방식을 모두 지원한다.

**A. 붙여넣기 방식**
- CSV/엑셀에서 `학번 이름` 두 열을 드래그해 그대로 붙여넣기
- 탭/쉼표/공백 구분자 자동 인식
- 붙여넣은 결과를 표로 미리 보여주고 확인 후 등록

**B. 번호 범위 생성 방식**
- "1번부터 28번까지" 형태로 학번 행을 먼저 만들고, 이름만 채워 넣기

> 학생 로그인은 학번만으로 하지만, 화면에 본인 이름이 표시되어 잘못 들어온 경우를 스스로 확인할 수 있어야 한다. 따라서 명단에는 학번과 이름이 함께 등록되어야 한다.

- 등록 후 개별 학생 추가·수정·삭제 가능

### 5.4 역할 설정
- 역할명과 **정원(명)** 을 교사가 직접 설정 (예: 급식부 3명, 칠판부 1명)
- 학급마다 역할 구성이 다르므로 완전 자유 편집

**프리셋 템플릿**
- 앱 진입 시 일반적인 1인 1역 목록이 기본으로 깔려 있음
- 교사는 필요 없는 항목을 지우고 정원만 고쳐서 즉시 사용 가능
- "백지에서 시작" 옵션도 함께 제공

**정원 검증**
- 역할 정원 총합 < 학생 수인 경우 경고 표시
  - 문구 예: *"역할 정원 총합(25명)이 학급 학생 수(28명)보다 적습니다. 3명이 배정받을 자리가 없습니다."*
- 경고에서 바로 정원 수정 화면으로 이동할 수 있어야 함
- 정원 총합 ≥ 학생 수인 경우는 정상. 남는 자리는 빈 자리로 둔다

### 5.5 지망 접수
- 교사가 접수를 **열고 닫을 수 있음** (오픈/마감 토글)
- 오픈 시 QR 코드와 짧은 접속 링크 생성 → 교실 화면에 띄워 학생에게 안내
- 마감 상태에서 학생이 접속하면 "접수가 마감되었습니다" 안내

**학생 화면**
1. 학번 입력 (별도 비밀번호 없음)
2. 등록된 이름 표시 → "본인이 맞나요?" 확인
3. 역할 목록에서 1지망·2지망·3지망 선택 (중복 선택 불가)
4. 제출 → 제출 완료 안내
5. 접수 마감 전이면 재접속해 수정 가능

**교사 실시간 현황**
- 제출 완료 / 미제출 학생을 실시간으로 목록 표시
- 미제출 학생 수를 상단에 크게 표시 (독려용)
- 역할별 지망 집중도(예: 도서부 1지망 7명)를 함께 보여주면 마감 판단에 도움

### 5.6 배정 실행

**알고리즘: 학급 전체 만족도 최대화 + 동점 시 무작위**

개인 간 경합을 하나씩 붙이는 대신, 학급 전체 조합을 놓고 1지망 배정 인원이 최대가 되는 조합을 찾는다. 만족도가 동일한 조합이 여럿일 때만 무작위로 정한다.

- 구현: 최소 비용 최대 유량 (min-cost max-flow) 또는 정원 확장 헝가리안 알고리즘
- 비용 설계: 1지망 = 0, 2지망 = 10, 3지망 = 100
  - 가중치를 크게 벌려두면 "2지망 여러 명을 희생해 1지망 한 명을 늘리는" 왜곡 없이 상위 지망 인원이 우선 최대화됨
- 동점 처리: 실행마다 학생 순서를 무작위로 섞어 입력 → 동일 비용의 여러 최적해 중 하나가 무작위로 선택됨
- 학생 30명 규모에서 즉시 계산 완료 (클라이언트 사이드 처리)

**학생에게 설명 가능한 문장**
> "컴퓨터가 반 전체에서 1지망을 받는 사람이 가장 많아지도록 계산했고, 똑같이 좋은 경우가 여러 개일 때는 뽑기로 정했어."

**미배정 처리**
- 1~3지망이 모두 채워져 배정받지 못한 학생 → **미배정으로 남긴다**
- 미제출 학생 → **미배정으로 남긴다**
- 자동으로 남은 역할에 밀어 넣지 않는다. 별도 **미배정 학생 리스트**로 모아 교사가 수기로 배정

### 5.7 배정안 비교 및 확정
교사는 한 번에 만족스러운 결과가 나오지 않을 수 있으므로 여러 번 실행한다.

- 실행할 때마다 배정안이 **저장되고 목록에 쌓임** (안 1, 안 2, 안 3…)
- 각 안마다 요약 지표 표시
  - 1지망 배정 인원 / 2지망 / 3지망 / 미배정
  - 예: `안 2 — 1지망 21명, 2지망 5명, 3지망 1명, 미배정 1명`
- 안을 나란히 비교하고 하나를 선택
- 선택한 안에서 **개별 학생의 역할을 수동으로 변경 가능** (정원 초과 시 경고)
- **최종 확정** 버튼 → 확정된 안은 잠기고, 필요 시 확정 해제 후 재편집

### 5.8 결과 공개 범위
- 배정 결과는 **교사 화면에만** 표시된다
- 학생 화면에서는 결과를 조회할 수 없다
  - 사유: 학생마다 확인 시점이 달라지면 교실에서 먼저 본 학생이 떠들며 분위기를 흐릴 수 있음
- 교사가 교실에서 직접 발표하는 것을 전제로 함
- (선택) 발표용 화면: 역할별로 배정된 학생을 큰 글씨로 보여주는 프로젝터용 뷰
- 결과 CSV 내보내기 / 인쇄

---

## 6. 기술 요구사항

### 6.1 스택
- 단일 `index.html` 파일, 빌드 도구 없음
- CDN only, 최상단 `CONFIG` 객체, 한국어 인라인 주석
- GitHub Pages 배포
- Firebase Auth (교사 Google 로그인) + Firestore

### 6.2 Firebase 프로젝트 운용
**기존 조회 도우미(Johoe) Firebase 프로젝트에 컬렉션을 추가하는 방식으로 구성한다.**

- 신규 프로젝트를 만들지 않는다 (무료 플랜 프로젝트 개수 한도 회피)
- Firestore는 컬렉션 단위로 완전히 분리되므로 기존 앱 데이터와 간섭하지 않는다
- 보안 규칙(Security Rules)에서 컬렉션별로 접근 경로를 분리해 상호 접근 차단
- 한 프로젝트에 여러 앱을 얹는 것은 Firebase의 정상적인 사용 방식

### 6.3 데이터 모델 (안)

```
roleAssign_classes/{classId}
  ├─ ownerUid: string          // 교사 계정 UID
  ├─ className: string
  ├─ submissionOpen: boolean
  ├─ createdAt / updatedAt
  │
  ├─ students/{studentId}
  │    ├─ number: string       // 학번
  │    └─ name: string
  │
  ├─ roles/{roleId}
  │    ├─ name: string
  │    └─ capacity: number
  │
  ├─ preferences/{studentNumber}
  │    ├─ choices: [roleId, roleId, roleId]
  │    └─ submittedAt
  │
  └─ drafts/{draftId}          // 배정안
       ├─ assignments: { studentNumber: roleId | null }
       ├─ stats: { first, second, third, unassigned }
       ├─ isFinal: boolean
       └─ createdAt
```

### 6.4 쓰기 정책
- **필드 단위 merge write를 사용한다.** 문서 전체 덮어쓰기 금지
  - 여러 학생이 동시에 지망을 제출할 때 last-write-wins로 인한 데이터 손실 방지
- 학생 제출은 `preferences/{학번}` 문서 단위로 독립 기록

### 6.5 보안 규칙
- `roleAssign_classes/{classId}` 읽기·쓰기: `ownerUid == request.auth.uid`
- `preferences` 하위: 미인증 사용자도 **자기 학번 문서 쓰기만** 허용, 단 `submissionOpen == true`일 때만
- 학생은 다른 학생의 지망이나 배정 결과를 읽을 수 없음
- 명단에 없는 학번은 제출 불가

---

## 7. 화면 목록

**교사**
1. 로그인
2. 내 학급 목록 / 학급 개설
3. 학생 명단 등록 (붙여넣기 / 범위 생성)
4. 역할·정원 설정 (프리셋 포함)
5. 지망 접수 관리 (QR 표시, 실시간 제출 현황)
6. 배정 실행 및 배정안 비교
7. 배정안 상세 — 수동 조정, 미배정 학생 리스트, 최종 확정
8. (선택) 발표용 전체 화면

**학생**
1. 학번 입력
2. 본인 확인
3. 1~3지망 선택
4. 제출 완료

---

## 8. 열린 질문 (v2 이후 검토)

- 학기 반복 사용 시 이전 역할 이력을 남겨 "지난번 3지망 받은 학생 우선" 가중치를 적용할 것인가
- 역할별 지망이 극단적으로 몰릴 때(예: 한 역할에 20명) 교사에게 재조사 권유 알림을 줄 것인가
- 학생 화면 결과 공개를 교사가 선택적으로 열 수 있게 할 것인가
- 학급 간 프리셋 공유 (동료 교사에게 역할 목록 템플릿 전달)
